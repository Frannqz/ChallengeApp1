import 'package:flutter/material.dart';

class ImageAgents {
  ImageAgents(this.image, this.color);
  final String image;
  final Color color;
}

class Agents {
  Agents(this.name, this.country, this.description);
  final String name;
  final String country;
  final String description;
}
